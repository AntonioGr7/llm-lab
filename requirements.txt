# Pinned to the versions the course was built and tested against, so the
# code keeps working even if you install months from now. Bump deliberately.

# Core deep learning
torch==2.12.0

# HuggingFace ecosystem (datasets, models, tokenizers)
transformers==5.9.0
tokenizers==0.22.2
datasets==4.8.5
accelerate==1.13.0
huggingface-hub==1.16.1

# Experiment tracking + config
wandb==0.27.0
pyyaml==6.0.3

# Numerics + utilities
einops==0.8.2

# Notebooks + plotting
jupyterlab==4.5.7
matplotlib==3.10.9


liger-kernel==0.8.0
pynvml==13.0.1
# Note: per-module dependencies (e.g. trl for DPO/GRPO, peft for LoRA)
# will be added as we introduce them in later modules.
